{
  "name": "cloudflare-pos-kasir",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:init": "wrangler d1 execute pos_db --file=./schema.sql",
    "check": "node --check src/worker.js"
  },
  "devDependencies": {
    "wrangler": "^3.83.0"
  }
}
